package server_client;

import javax.swing.JFrame;

import game.GameRunner;
import game.Player;
import game.Room;
import game.WorldObject;
import render.RenderPanel;
import topLevel.GameWindow;

public class Main2 {
	 public static void main(String args[]){
		 GameWindow window = new GameWindow();
		/* GameRunner game = new GameRunner();
		 Player player = game.createPlayer("bob", "is an idiot");
		 JFrame frame = new JFrame("game");
		 
		 Room room = game.getPlayerRoom(player);
		 for(WorldObject o:room.getWorldObjects())
				System.out.print(o+"  ");
		 

		 RendererRemade render = new RendererRemade(game.getPlayerRoom(player));
		 System.out.println(player+"    "+game.getPlayerRoom(player));
		 render.setSize(1000,1000);
		// render.setVisible(true);
		 frame.add(render);
		 frame.setSize(1000,1000);

		 frame.setVisible(true);*/
	 }
}
